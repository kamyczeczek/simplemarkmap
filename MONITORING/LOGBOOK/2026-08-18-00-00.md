# Logbook Entry: 2026-08-18

## The Aim
Enable subtree drag-and-drop by wiring node `mousedown` events to the existing `dragState.mousedown` handler.

## What It Was Told
- `render()` called nonexistent `onNodeMouseDown`.
- The drag implementation already handles thresholding, cycle prevention, depth updates, and autosave.

## How It Went
- Changed `public/index.html` to call `dragState.mousedown(e, div)`.
- Added the corresponding append-only rule to `harness/error-rules.md`.
- Verified the source wiring and existing drop path calls `setSubtreeDepth` and `scheduleAutoSave()`.
- Ran `npm run harness`: constraints passed and 18 unit tests passed.

## Error Report
Recorded in `harness/error-rules.md`: `onNodeMouseDown` ReferenceError caused by calling a nonexistent global handler.

## The Verdict
PASS — 0 issues.
